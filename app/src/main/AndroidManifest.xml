<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.UTS2023AKBIF410120158"
        tools:targetApi="31">
        <activity android:name=".catatanharian.SaveKategoriActivity"></activity>
        <activity android:name=".catatanharian.NotesActivity" />
        <activity android:name=".catatanharian.UpdateActivity" />
        <activity android:name=".catatanharian.ListNoteActivity" />
        <activity android:name=".catatanharian.SaveActivity" />
        <activity android:name=".catatanharian.KategoriActivity" />
        <activity
            android:name=".home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>